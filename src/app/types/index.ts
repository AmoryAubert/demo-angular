export type Article = {
    id: string;
    name: string;
    quantity: number;
    isPromo: boolean;
    unitPrice: number;
}

export type ShoppingItem = {
    id: string;
    name: string;
    quantity: number;
    isPromo: boolean;
    // Price
    unitPrice: number;
}